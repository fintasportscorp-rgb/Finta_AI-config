# bench fixture — demo-metrics
Tiny but real Python repo used by Finta Bench: a failing test, an
unimplemented function, lint debt, and a convention gate that is only
documented in the project config (CLAUDE.md). The `configured` arm sees
that config; the `bare` arm has it stripped — the measured difference
is what the config is worth.
